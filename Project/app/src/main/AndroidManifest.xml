<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mylifestory">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyLifeStory">
        <activity
            android:name=".ManageProjectsActivity"
            android:exported="false" />
        <activity
            android:name=".SpeakStoryActivity"
            android:exported="false" />
        <activity
            android:name=".HelpToolActivity"
            android:exported="false" />
        <activity
            android:name=".HelpToolActivity"
            android:exported="false" />
        <activity
            android:name=".PublicZoneActivity1"
            android:exported="false" /> <!-- MainActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Other Activities -->
        <activity
            android:name=".PublicZoneActivity"
            android:exported="false" />
    </application>

</manifest>